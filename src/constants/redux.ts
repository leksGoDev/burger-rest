export enum SocketStoreName {
    feed = "feedSocketApi",
    history = "historySocketApi"
}

export enum SocketStartActionType {
    feed = "feedSocketApi/start",
    history = "historySocketApi/start"
}