import type { FC } from 'react';

import styles from "./feed.module.css";

const Feed: FC = () => {

    return (
        <section className={styles.main}>
            <header></header>
            <main></main>
        </section>
    );
};

export default Feed;
